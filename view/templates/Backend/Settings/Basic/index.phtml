<?php
/**
 * 基础设置页面（内容模板）
 * 
 * 只包含页面内容，由布局系统自动包裹 HTML 框架
 * 布局由 BackendController::$layoutType = 'default.default' 控制
 *
 * @var \Weline\Framework\View\Template $this
 * @var array $settings
 */
$settings = $settings ?? [];
$backendDefaultLanguage = (string)($settings['language'] ?? 'zh_Hans_CN');
?>
<!-- 页面标题 -->
<div class="row">
    <div class="col-12">
        <div class="page-title-box d-sm-flex align-items-center justify-content-between">
            <h4 class="mb-sm-0 font-size-18"><lang>基础设置</lang></h4>
        </div>
    </div>
</div>

<!-- 消息提示区域 -->
<div class='row'>
    <div class="col-12">
        <template>Weline_Component::message.phtml</template>
    </div>
</div>

<!-- 提示信息 -->
<div class="row">
    <div class="col-12">
        <div class="alert alert-info">
            <i class="mdi mdi-information me-2"></i>
            <strong><lang>提示</lang>:</strong> <lang>基础设置功能正在开发中，敬请期待。</lang>
        </div>
    </div>
</div>

<!-- 设置表单 -->
<div class="row">
    <div class="col-xl-12">
        <div class="card">
            <div class="card-body">
                <h4 class="card-title mb-4"><lang>基础设置</lang></h4>
                
                <w:form id="settingsForm">
                    <div class="row">
                        <div class="col-md-6">
                            <div class="mb-3">
                                <label class="form-label"><lang>站点名称</lang></label>
                                <input type="text" class="form-control" name="site_name" 
                                       value="<?= htmlspecialchars($settings['site_name'] ?? '') ?>">
                            </div>
                        </div>
                        <div class="col-md-6">
                            <div class="mb-3">
                                <label class="form-label"><lang>站点URL</lang></label>
                                <input type="url" class="form-control" name="site_url" 
                                       value="<?= htmlspecialchars($settings['site_url'] ?? '') ?>">
                            </div>
                        </div>
                        <div class="col-md-6">
                            <div class="mb-3">
                                <label class="form-label"><lang>时区</lang></label>
                                <select class="form-select" name="timezone">
                                    <option value="Asia/Shanghai" <?= ($settings['timezone'] ?? '') === 'Asia/Shanghai' ? 'selected' : '' ?>>
                                        Asia/Shanghai
                                    </option>
                                    <option value="UTC" <?= ($settings['timezone'] ?? '') === 'UTC' ? 'selected' : '' ?>>
                                        UTC
                                    </option>
                                </select>
                            </div>
                        </div>
                        <div class="col-md-6">
                            <div class="mb-3">
                                <label class="form-label"><lang>默认语言</lang></label>
                                <w:i18n:language:select
                                    id="backend_basic_default_language_selector"
                                    name="language"
                                    input-id="language"
                                    value="backendDefaultLanguage"
                                    required="true"
                                    class="w-100"
                                />
                                <?php if (false): ?>
                                <select class="form-select" name="language">
                                    <option value="zh_Hans_CN" <?= ($settings['language'] ?? '') === 'zh_Hans_CN' ? 'selected' : '' ?>>
                                        简体中文
                                    </option>
                                    <option value="en_US" <?= ($settings['language'] ?? '') === 'en_US' ? 'selected' : '' ?>>
                                        English
                                    </option>
                                </select>
                                <?php endif; ?>
                            </div>
                        </div>
                        <div class="col-md-12">
                            <button type="submit" class="btn btn-primary">
                                <i class="mdi mdi-content-save me-2"></i><lang>保存设置</lang>
                            </button>
                        </div>
                    </div>
                </w:form>
            </div>
        </div>
    </div>
</div>


<script>
(function(g){
  if(g.__bqAdmin_backend_admin) return;
  g.__bqAdmin_backend_admin=true;
  g.bqAdmin=g.bqAdmin||{};
  g.bqAdmin['backend_admin']=function(url, options){
    options=options||{};
    var body=options.body;
    if(body && typeof FormData!=='undefined' && body instanceof FormData){
      var p=new URLSearchParams(); body.forEach(function(v,k){ if(!(typeof File!=='undefined'&&v instanceof File)) p.append(k,String(v)); }); body=p.toString();
    } else if(body && typeof body!=='string'){ try{ body=JSON.stringify(body); }catch(e){ body=''; } }
    var run=function(api){ return api.resource('backend_admin').adminRequest({url:url, method:options.method||'POST', headers:options.headers||{}, body:body||''}); };
    var toResp=function(data){
      var _biz=g.WelineApiBusiness||(g.Weline&&g.Weline.ApiBusiness);
      if(_biz&&typeof _biz.wrapAdminBridgeResult==='function'){
        return _biz.wrapAdminBridgeResult(data);
      }
      var body=(data&&typeof data==='object'&&!Array.isArray(data))?data:{success:true,data:data};
      var ok=!(body&&body.success===false);
      var resp={ok:ok,status:ok?200:400,json:function(){return Promise.resolve(body);},text:function(){return Promise.resolve(typeof body==='string'?body:JSON.stringify(body==null?{}:body));}};
      Object.keys(body).forEach(function(k){ if(k==='ok'||k==='json'||k==='text'||k==='status') return; resp[k]=body[k]; });
      return resp;
    };
    var p=(g.Weline&&g.Weline.load)?g.Weline.load('api').then(run):Promise.resolve(run(g.Weline.Api));
    return p.then(toResp);
  };
})(window);
</script>
<script>
(function() {
    'use strict';
    
    window.WelineBasicSettings = {
        save: function(event) {
            event.preventDefault();
            var formData = new FormData(event.target);
            bqAdmin['backend_admin']('<?= $this->getBackendUrl('*/backend/settings/basic/save') ?>', { method: 'POST', body: formData })
            .then(function(response) {
                var data = (function(r){ var b=window.WelineApiBusiness||(window.Weline&&window.Weline.ApiBusiness); return (b&&typeof b.unwrapBusiness==='function')?b.unwrapBusiness(r):r.data; })(response);
                var Toast = window.BackendToast || window.BackendToast;
                if (response.ok && data && data.success) {
                    Toast.success(data.message || '<?= __('保存成功') ?>');
                } else {
                    Toast.error((data && data.message) || '<?= __('保存失败') ?>');
                }
            })
            .catch(function(error) {
                (window.BackendToast || window.BackendToast).error(error && error.message ? error.message : '<?= __('保存失败') ?>');
            });
        }
    };

    var settingsForm = document.getElementById('settingsForm');
    if (settingsForm) {
        settingsForm.addEventListener('submit', window.WelineBasicSettings.save);
    }
})();
</script>
