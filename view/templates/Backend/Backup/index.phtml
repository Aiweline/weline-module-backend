<?php
/**
 * 系统备份管理页面（内容模板）
 * 
 * 只包含页面内容，由布局系统自动包裹 HTML 框架
 * 布局由 BackendController::$layoutType = 'default.default' 控制
 *
 * @var \Weline\Framework\View\Template $this
 * @var array $backups
 */
$backups = $backups ?? [];
?>
<!-- 页面标题 -->
<div class="row">
    <div class="col-12">
        <div class="page-title-box d-sm-flex align-items-center justify-content-between">
            <h4 class="mb-sm-0 font-size-18"><lang>系统备份</lang></h4>
        </div>
    </div>
</div>

<!-- 消息提示区域 -->
<div class='row'>
    <div class="col-12">
        <template>Weline_Component::message.phtml</template>
    </div>
</div>

<!-- 提示信息 -->
<div class="row">
    <div class="col-12">
        <div class="alert alert-info">
            <i class="mdi mdi-information me-2"></i>
            <strong><lang>提示</lang>:</strong> <lang>系统备份功能正在开发中，敬请期待。</lang>
        </div>
    </div>
</div>

<!-- 创建备份 -->
<div class="row">
    <div class="col-xl-12">
        <div class="card">
            <div class="card-body">
                <h4 class="card-title mb-4"><lang>创建备份</lang></h4>
                
                <form id="backupForm" onsubmit="WelineBackup.createBackup(event)">
                    <div class="row">
                        <div class="col-md-6">
                            <div class="mb-3">
                                <label class="form-label"><lang>备份类型</lang></label>
                                <select class="form-select" id="backup_type" name="backup_type">
                                    <option value="full"><lang>完整备份</lang></option>
                                    <option value="database"><lang>仅数据库</lang></option>
                                    <option value="files"><lang>仅文件</lang></option>
                                </select>
                            </div>
                        </div>
                        <div class="col-md-6">
                            <div class="mb-3">
                                <label class="form-label">&nbsp;</label>
                                <div>
                                    <button type="submit" class="btn btn-primary">
                                        <i class="mdi mdi-content-save me-2"></i><lang>创建备份</lang>
                                    </button>
                                </div>
                            </div>
                        </div>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>

<!-- 备份列表 -->
<div class="row">
    <div class="col-xl-12">
        <div class="card">
            <div class="card-body">
                <h4 class="card-title mb-4"><lang>备份列表</lang></h4>
                
                <?php if (!empty($backups)): ?>
                <div class="table-responsive">
                    <table class="table table-hover table-striped align-middle table-nowrap mb-0">
                        <thead class="table-light">
                            <tr>
                                <th><lang>备份文件</lang></th>
                                <th><lang>类型</lang></th>
                                <th><lang>大小</lang></th>
                                <th><lang>创建时间</lang></th>
                                <th><lang>操作</lang></th>
                            </tr>
                        </thead>
                        <tbody>
                            <?php foreach ($backups as $backup): ?>
                            <tr>
                                <td><?= htmlspecialchars($backup['filename'] ?? '-') ?></td>
                                <td><?= htmlspecialchars($backup['type'] ?? '-') ?></td>
                                <td><?= $this->formatBytes($backup['size'] ?? 0) ?></td>
                                <td><?= $backup['created_at'] ?? '-' ?></td>
                                <td>
                                    <button type="button" class="btn btn-sm btn-primary" onclick="WelineBackup.downloadBackup('<?= $backup['filename'] ?? '' ?>')">
                                        <i class="mdi mdi-download me-1"></i><lang>下载</lang>
                                    </button>
                                    <button type="button" class="btn btn-sm btn-danger" onclick="WelineBackup.deleteBackup('<?= $backup['filename'] ?? '' ?>')">
                                        <i class="mdi mdi-delete me-1"></i><lang>删除</lang>
                                    </button>
                                </td>
                            </tr>
                            <?php endforeach; ?>
                        </tbody>
                    </table>
                </div>
                <?php else: ?>
                <div class="text-center text-muted py-5">
                    <i class="mdi mdi-backup-restore" style="font-size: 48px;"></i>
                    <p class="mt-3"><lang>暂无备份文件</lang></p>
                </div>
                <?php endif; ?>
            </div>
        </div>
    </div>
</div>

<script>
(function() {
    'use strict';
    
    window.WelineBackup = {
        createBackup: function(event) {
            event.preventDefault();
            var formData = new FormData(event.target);
            
            var url = '<?= $this->getBackendUrl('*/backup/create') ?>';
            var opts = { method: 'POST', body: formData };
            window.Weline.Api.request(url, opts)
            .then(function(res) {
                var data = (res && res.data) !== undefined ? res.data : res;
                if (data.success) {
                    BackendToast.success(data.message);
                    setTimeout(function() {
                        location.reload();
                    }, 1000);
                } else {
                    BackendToast.error(data.message);
                }
            })
            .catch(function(error) {
                BackendToast.error('<?= __('创建备份失败') ?>');
            });
        },
        
        downloadBackup: function(filename) {
            BackendToast.info('<?= __('下载备份：') ?>' + filename);
        },
        
        deleteBackup: function(filename) {
            AdminConfirm.show('<?= __('确定要删除这个备份吗？') ?>').then(function(confirmed) {
                if (confirmed) {
                    BackendToast.info('<?= __('删除备份：') ?>' + filename);
                }
            });
        }
    };
})();
</script>
