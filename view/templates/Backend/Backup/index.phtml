<?php
/**
 * 系统备份管理页面（内容模板）
 * 
 * 只包含页面内容，由布局系统自动包裹 HTML 框架
 * 布局由 BackendController::$layoutType = 'default.default' 控制
 *
 * @var \Weline\Framework\View\Template $this
 * @var array $backups
 */
$backups = $backups ?? [];
$jsonJsLiteral = static function (mixed $value): string {
    $json = \json_encode(
        (string)$value,
        JSON_UNESCAPED_UNICODE | JSON_HEX_TAG | JSON_HEX_AMP | JSON_HEX_APOS | JSON_HEX_QUOT
    ) ?: '""';
    return htmlspecialchars($json, ENT_QUOTES, 'UTF-8');
};
?>
<!-- 页面标题 -->
<div class="row">
    <div class="col-12">
        <div class="page-title-box d-sm-flex align-items-center justify-content-between">
            <h4 class="mb-sm-0 font-size-18"><lang>系统备份</lang></h4>
        </div>
    </div>
</div>

<!-- 消息提示区域 -->
<div class='row'>
    <div class="col-12">
        <template>Weline_Component::message.phtml</template>
    </div>
</div>

<!-- 提示信息 -->
<div class="row">
    <div class="col-12">
        <div class="alert alert-info">
            <i class="mdi mdi-information me-2"></i>
            <strong><lang>提示</lang>:</strong> <lang>系统备份功能正在开发中，敬请期待。</lang>
        </div>
    </div>
</div>

<!-- 创建备份 -->
<div class="row">
    <div class="col-xl-12">
        <div class="card">
            <div class="card-body">
                <h4 class="card-title mb-4"><lang>创建备份</lang></h4>
                
                <w:form id="backupForm">
                    <div class="row">
                        <div class="col-md-6">
                            <div class="mb-3">
                                <label class="form-label"><lang>备份类型</lang></label>
                                <select class="form-select" id="backup_type" name="backup_type">
                                    <option value="full"><lang>完整备份</lang></option>
                                    <option value="database"><lang>仅数据库</lang></option>
                                    <option value="files"><lang>仅文件</lang></option>
                                </select>
                            </div>
                        </div>
                        <div class="col-md-6">
                            <div class="mb-3">
                                <label class="form-label">&nbsp;</label>
                                <div>
                                    <button type="submit" class="btn btn-primary">
                                        <i class="mdi mdi-content-save me-2"></i><lang>创建备份</lang>
                                    </button>
                                </div>
                            </div>
                        </div>
                    </div>
                </w:form>
            </div>
        </div>
    </div>
</div>

<!-- 备份列表 -->
<div class="row">
    <div class="col-xl-12">
        <div class="card">
            <div class="card-body">
                <h4 class="card-title mb-4"><lang>备份列表</lang></h4>
                
                <?php if (!empty($backups)): ?>
                <div class="table-responsive">
                    <table class="table table-hover table-striped align-middle table-nowrap mb-0">
                        <thead class="table-light">
                            <tr>
                                <th><lang>备份文件</lang></th>
                                <th><lang>类型</lang></th>
                                <th><lang>大小</lang></th>
                                <th><lang>创建时间</lang></th>
                                <th><lang>操作</lang></th>
                            </tr>
                        </thead>
                        <tbody>
                            <?php foreach ($backups as $backup): ?>
                            <?php $backupFilename = (string)($backup['filename'] ?? ''); ?>
                            <tr>
                                <td><?= htmlspecialchars($backup['filename'] ?? '-') ?></td>
                                <td><?= htmlspecialchars($backup['type'] ?? '-') ?></td>
                                <td><?= $this->formatBytes($backup['size'] ?? 0) ?></td>
                                <td><?= htmlspecialchars((string)($backup['created_at'] ?? '-')) ?></td>
                                <td>
                                    <button type="button" class="btn btn-sm btn-primary"
                                            data-backup-action="download"
                                            data-backup-filename="<?= htmlspecialchars($backupFilename, ENT_QUOTES, 'UTF-8') ?>">
                                        <i class="mdi mdi-download me-1"></i><lang>下载</lang>
                                    </button>
                                    <button type="button" class="btn btn-sm btn-danger"
                                            data-backup-action="delete"
                                            data-backup-filename="<?= htmlspecialchars($backupFilename, ENT_QUOTES, 'UTF-8') ?>">
                                        <i class="mdi mdi-delete me-1"></i><lang>删除</lang>
                                    </button>
                                </td>
                            </tr>
                            <?php endforeach; ?>
                        </tbody>
                    </table>
                </div>
                <?php else: ?>
                <div class="text-center text-muted py-5">
                    <i class="mdi mdi-backup-restore" style="font-size: 48px;"></i>
                    <p class="mt-3"><lang>暂无备份文件</lang></p>
                </div>
                <?php endif; ?>
            </div>
        </div>
    </div>
</div>


<script>
(function(g){
  if(g.__bqAdmin_backend_admin) return;
  g.__bqAdmin_backend_admin=true;
  g.bqAdmin=g.bqAdmin||{};
  g.bqAdmin['backend_admin']=function(url, options){
    options=options||{};
    var body=options.body;
    if(body && typeof FormData!=='undefined' && body instanceof FormData){
      var p=new URLSearchParams(); body.forEach(function(v,k){ if(!(typeof File!=='undefined'&&v instanceof File)) p.append(k,String(v)); }); body=p.toString();
    } else if(body && typeof body!=='string'){ try{ body=JSON.stringify(body); }catch(e){ body=''; } }
    var run=function(api){ return api.resource('backend_admin').adminRequest({url:url, method:options.method||'POST', headers:options.headers||{}, body:body||''}); };
    var toResp=function(data){
      var _biz=g.WelineApiBusiness||(g.Weline&&g.Weline.ApiBusiness);
      if(_biz&&typeof _biz.wrapAdminBridgeResult==='function'){
        return _biz.wrapAdminBridgeResult(data);
      }
      var body=(data&&typeof data==='object'&&!Array.isArray(data))?data:{success:true,data:data};
      var ok=!(body&&body.success===false);
      var resp={ok:ok,status:ok?200:400,json:function(){return Promise.resolve(body);},text:function(){return Promise.resolve(typeof body==='string'?body:JSON.stringify(body==null?{}:body));}};
      Object.keys(body).forEach(function(k){ if(k==='ok'||k==='json'||k==='text'||k==='status') return; resp[k]=body[k]; });
      return resp;
    };
    var p=(g.Weline&&g.Weline.load)?g.Weline.load('api').then(run):Promise.resolve(run(g.Weline.Api));
    return p.then(toResp);
  };
})(window);
</script>
<script>
(function() {
    'use strict';
    
    window.WelineBackup = {
        createBackup: function(event) {
            event.preventDefault();
            var formData = new FormData(event.target);
            
            var url = '<?= $this->getBackendUrl('*/backup/create') ?>';
            var opts = { method: 'POST', body: formData };
            bqAdmin['backend_admin'](url, opts)
            .then(function(res) {
                var data = (res && res.data) !== undefined ? res.data : res;
                if (data.success) {
                    BackendToast.success(data.message);
                    setTimeout(function() {
                        location.reload();
                    }, 1000);
                } else {
                    BackendToast.error(data.message);
                }
            })
            .catch(function(error) {
                BackendToast.error('<?= __('创建备份失败') ?>');
            });
        },
        
        downloadBackup: function(filename) {
            BackendToast.info('<?= __('下载备份：') ?>' + filename);
        },
        
        deleteBackup: function(filename) {
            AdminConfirm.show('<?= __('确定要删除这个备份吗？') ?>').then(function(confirmed) {
                if (confirmed) {
                    BackendToast.info('<?= __('删除备份：') ?>' + filename);
                }
            });
        }
    };

    var backupForm = document.getElementById('backupForm');
    if (backupForm) {
        backupForm.addEventListener('submit', window.WelineBackup.createBackup);
    }

    document.addEventListener('click', function(event) {
        var trigger = event.target.closest('[data-backup-action]');
        if (!trigger) {
            return;
        }

        event.preventDefault();
        var filename = trigger.getAttribute('data-backup-filename') || '';
        if (trigger.getAttribute('data-backup-action') === 'download') {
            window.WelineBackup.downloadBackup(filename);
            return;
        }

        if (trigger.getAttribute('data-backup-action') === 'delete') {
            window.WelineBackup.deleteBackup(filename);
        }
    });
})();
</script>
