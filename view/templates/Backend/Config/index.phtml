<?php
/**
 * 后台配置页面（内容模板）
 * 
 * 只包含页面内容，由布局系统自动包裹 HTML 框架
 * 布局由 BackendController::$layoutType = 'default.default' 控制
 *
 * @var \Weline\Framework\View\Template $this
 * @var array $configs
 */
$configs = $configs ?? [];
$usesDefaultLogo = trim((string)($configs['logo_dark'] ?? '')) === ''
    && trim((string)($configs['logo_light'] ?? '')) === ''
    && trim((string)($configs['logo_sm'] ?? '')) === '';
?>
<!-- 页面标题 -->
<div class="row">
    <div class="col-12">
        <div class="page-title-box d-sm-flex align-items-center justify-content-between">
            <h4 class="mb-sm-0 font-size-18"><lang>外观与 Logo</lang></h4>
        </div>
    </div>
</div>

<!-- 消息提示区域 -->
<div class='row'>
    <div class="col-12">
        <template>Weline_Component::message.phtml</template>
    </div>
</div>

<!-- 配置表单 -->
<div class="row">
    <div class="col-xl-12">
        <div class="card">
            <div class="card-body">
                <h4 class="card-title mb-4"><lang>外观与 Logo</lang></h4>
                <p class="text-muted mb-4"><lang>配置全站 Logo 与站点图标：未填写 Logo 时默认使用与登录页一致的 W 字母黄色丝带标识；填写后前后台顶栏、登录页与浏览器标签页将统一使用此处配置。</lang></p>
                <?php if ($usesDefaultLogo): ?>
                    <div class="alert alert-light border d-flex align-items-center gap-3 mb-4" role="status">
                        <img src="/Weline/Theme/view/theme/backend/assets/images/theme/logo.png"
                             alt="<?= __('默认 Weline Logo') ?>"
                             width="96"
                             height="75"
                             style="object-fit: contain;">
                        <div>
                            <strong class="d-block"><lang>当前使用默认 Logo</lang></strong>
                            <span class="text-muted"><lang>未配置自定义 Logo，后台顶栏与登录页使用同一个 W 字母黄色丝带标识。</lang></span>
                        </div>
                    </div>
                <?php endif; ?>
                
                <w:form id="configForm">
                    <!-- Logo配置 -->
                    <div class="mb-4">
                        <h5 class="mb-3"><lang>Logo配置</lang></h5>
                        
                        <div class="row">
                            <div class="col-md-6">
                                <div class="mb-3">
                                    <label class="form-label"><lang>深色Logo（大图）</lang></label>
                                    <input type="hidden" class="form-control" id="logo_dark" name="config[logo_dark]" 
                                           value="{{configs.logo_dark}}">
                                    <file-manager target='#logo_dark'
                                                  title='@lang(选择深色Logo)'
                                                  vars='configs'
                                                  preview="1"
                                                  path='backend/logo/'
                                                  value='configs.logo_dark'
                                                  multi='0'
                                                  ext="jpg,jpeg,png,gif,webp,svg"
                                                  size="2048000"
                                                  w='70'
                                                  h='70'/>
                                    <small class="form-text text-muted">
                                        <lang>用于深色主题的大尺寸Logo，建议高度70px</lang>
                                    </small>
                                </div>
                            </div>
                            <div class="col-md-6">
                                <div class="mb-3">
                                    <label class="form-label"><lang>浅色Logo（大图）</lang></label>
                                    <input type="hidden" class="form-control" id="logo_light" name="config[logo_light]" 
                                           value="{{configs.logo_light}}">
                                    <file-manager target='#logo_light'
                                                  title='@lang(选择浅色Logo)'
                                                  vars='configs'
                                                  preview="1"
                                                  path='backend/logo/'
                                                  value='configs.logo_light'
                                                  multi='0'
                                                  ext="jpg,jpeg,png,gif,webp,svg"
                                                  size="2048000"
                                                  w='70'
                                                  h='70'/>
                                    <small class="form-text text-muted">
                                        <lang>用于浅色主题的大尺寸Logo，建议高度70px</lang>
                                    </small>
                                </div>
                            </div>
                            <div class="col-md-6">
                                <div class="mb-3">
                                    <label class="form-label"><lang>小Logo</lang></label>
                                    <input type="hidden" class="form-control" id="logo_sm" name="config[logo_sm]" 
                                           value="{{configs.logo_sm}}">
                                    <file-manager target='#logo_sm'
                                                  title='@lang(选择小Logo)'
                                                  vars='configs'
                                                  preview="1"
                                                  path='backend/logo/'
                                                  value='configs.logo_sm'
                                                  multi='0'
                                                  ext="jpg,jpeg,png,gif,webp,svg"
                                                  size="2048000"
                                                  w='50'
                                                  h='50'/>
                                    <small class="form-text text-muted">
                                        <lang>用于侧边栏折叠时的小尺寸Logo，建议高度50px</lang>
                                    </small>
                                </div>
                            </div>

                            <div class="col-md-6">
                                <div class="mb-3">
                                    <label class="form-label"><lang>站点图标（Favicon）</lang></label>
                                    <input type="hidden" class="form-control" id="site_icon" name="config[site_icon]"
                                           value="{{configs.site_icon}}">
                                    <file-manager target='#site_icon'
                                                  title='@lang(选择站点图标)'
                                                  vars='configs'
                                                  preview="1"
                                                  path='backend/icon/'
                                                  value='configs.site_icon'
                                                  multi='0'
                                                  ext="jpg,jpeg,png,gif,webp,svg,ico"
                                                  size="1024000"
                                                  w='128'
                                                  h='128'/>
                                    <small class="form-text text-muted">
                                        <lang>前后台浏览器标签页图标；留空则使用 Theme 默认 icon（与 Logo 同源）。</lang>
                                    </small>
                                </div>
                            </div>
                        </div>
                    </div>

                    <hr>

                    <!-- 登录页背景 -->
                    <div class="mb-4">
                        <h5 class="mb-3"><lang>登录页背景</lang></h5>
                        <div class="row">
                            <div class="col-md-6">
                                <div class="mb-3">
                                    <label class="form-label"><lang>登录页背景图</lang></label>
                                    <input type="hidden" class="form-control" id="login_bg" name="config[login_bg]"
                                           value="{{configs.login_bg}}">
                                    <file-manager target='#login_bg'
                                                  title='@lang(选择登录页背景)'
                                                  vars='configs'
                                                  preview="1"
                                                  path='backend/bg/'
                                                  value='configs.login_bg'
                                                  multi='0'
                                                  ext="jpg,jpeg,png,gif,webp"
                                                  size="5120000"/>
                                    <small class="form-text text-muted">
                                        <lang>建议尺寸 1920x1080，留空使用默认背景</lang>
                                    </small>
                                </div>
                            </div>
                        </div>
                    </div>

                    <hr>

                    <!-- 站点信息配置 -->
                    <div class="mb-4">
                        <h5 class="mb-3"><lang>站点信息</lang></h5>
                        
                        <div class="row">
                            <div class="col-md-6">
                                <div class="mb-3">
                                    <label class="form-label"><lang>站点名称</lang></label>
                                    <input type="text" class="form-control" name="config[site_name]" 
                                           value="<?= htmlspecialchars($configs['site_name'] ?? '') ?>">
                                    <small class="form-text text-muted">
                                        <lang>后台管理系统的名称</lang>
                                    </small>
                                </div>
                            </div>
                            <div class="col-md-6">
                                <div class="mb-3">
                                    <label class="form-label"><lang>站点描述</lang></label>
                                    <input type="text" class="form-control" name="config[site_description]" 
                                           value="<?= htmlspecialchars($configs['site_description'] ?? '') ?>">
                                    <small class="form-text text-muted">
                                        <lang>后台管理系统的描述信息</lang>
                                    </small>
                                </div>
                            </div>
                        </div>
                    </div>

                    <div class="mt-4">
                        <button type="submit" class="btn btn-primary">
                            <i class="mdi mdi-content-save me-2"></i><lang>保存配置</lang>
                        </button>
                        <button type="button" class="btn btn-secondary ms-2" data-config-action="reset">
                            <i class="mdi mdi-refresh me-2"></i><lang>重置</lang>
                        </button>
                    </div>
                </w:form>
            </div>
        </div>
    </div>
</div>


<script>
(function(g){
  if(g.__bqAdmin_backend_admin) return;
  g.__bqAdmin_backend_admin=true;
  g.bqAdmin=g.bqAdmin||{};
  g.bqAdmin['backend_admin']=function(url, options){
    options=options||{};
    var body=options.body;
    if(body && typeof FormData!=='undefined' && body instanceof FormData){
      var p=new URLSearchParams(); body.forEach(function(v,k){ if(!(typeof File!=='undefined'&&v instanceof File)) p.append(k,String(v)); }); body=p.toString();
    } else if(body && typeof body!=='string'){ try{ body=JSON.stringify(body); }catch(e){ body=''; } }
    var run=function(api){ return api.resource('backend_admin').adminRequest({url:url, method:options.method||'POST', headers:options.headers||{}, body:body||''}); };
    var toResp=function(data){
      var _biz=g.WelineApiBusiness||(g.Weline&&g.Weline.ApiBusiness);
      if(_biz&&typeof _biz.wrapAdminBridgeResult==='function'){
        return _biz.wrapAdminBridgeResult(data);
      }
      var body=(data&&typeof data==='object'&&!Array.isArray(data))?data:{success:true,data:data};
      var ok=!(body&&body.success===false);
      var resp={ok:ok,status:ok?200:400,json:function(){return Promise.resolve(body);},text:function(){return Promise.resolve(typeof body==='string'?body:JSON.stringify(body==null?{}:body));}};
      Object.keys(body).forEach(function(k){ if(k==='ok'||k==='json'||k==='text'||k==='status') return; resp[k]=body[k]; });
      return resp;
    };
    var p=(g.Weline&&g.Weline.load)?g.Weline.load('api').then(run):Promise.resolve(run(g.Weline.Api));
    return p.then(toResp);
  };
})(window);
</script>
<script>
(function() {
    'use strict';
    
    window.saveConfig = function(event) {
        event.preventDefault();
        const form = document.getElementById('configForm');
        const formData = new FormData(form);
        const data = {};
        
        for (let [key, value] of formData.entries()) {
            if (key.startsWith('config[')) {
                const configKey = key.match(/config\[(.*?)\]/)[1];
                data['config[' + configKey + ']'] = value;
            } else {
                data[key] = value;
            }
        }
        
        var url = '<?= $this->getBackendUrl('*/backend/config/save') ?>';
        var opts = { method: 'POST', headers: { 'Content-Type': 'application/x-www-form-urlencoded' }, body: new URLSearchParams(data).toString() };
        bqAdmin['backend_admin'](url, opts)
        .then(function(res) {
            var data = (res && res.data) !== undefined ? res.data : res;
            if (data.success) {
                BackendToast.success(data.message);
                setTimeout(() => {
                    window.location.reload();
                }, 1000);
            } else {
                BackendToast.error(data.message);
            }
        })
        .catch(error => {
            console.error('Error:', error);
            BackendToast.error('<?= __('保存失败') ?>');
        });
    };

    window.resetForm = function() {
        AdminConfirm.show('<?= __('确定要重置表单吗？') ?>').then(function(confirmed) {
            if (confirmed) {
                document.getElementById('configForm').reset();
            }
        });
    };

    var configForm = document.getElementById('configForm');
    if (configForm) {
        configForm.addEventListener('submit', window.saveConfig);
    }

    document.addEventListener('click', function(event) {
        var trigger = event.target.closest('[data-config-action="reset"]');
        if (!trigger) {
            return;
        }

        event.preventDefault();
        window.resetForm();
    });
})();
</script>
